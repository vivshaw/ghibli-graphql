type Person {
  id: String!
  name: String!
  gender: String!
  age: String!
  eye_color: String!
  hair_color: String!
  films: [Film]!
  species: Species!
}

type Query {
  people: [Person]
  person(id: String!): Person
}
