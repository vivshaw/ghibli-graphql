type Location {
  id: String!
  name: String!
  climate: String!
  terrain: String!
  surface_water: Int!
  films: [Film]!
  residents: [Person]
}

type Query {
  locations: [Location]
  location(id: String!): Location
}
