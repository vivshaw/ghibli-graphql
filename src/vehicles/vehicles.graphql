type Vehicle {
  id: String!
  name: String!
  description: String!
  vehicle_class: String!
  length: Int!
  pilot: Person!
  film: Film!
}

type Query {
  vehicles: [Vehicle]
  vehicle(id: String!): Vehicle
}
