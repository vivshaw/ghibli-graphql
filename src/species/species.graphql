type Species {
  id: String!
  name: String!
  classification: String!
  eye_colors: String!
  hair_colors: String!
  films: [Film]!
  people: [Person]!
}

type Query {
  species: [Species]
  specie(id: String!): Species
}
